<h1>Liste des restaurants à Lille</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>

<%= link_to "Add a new restaurant", new_restaurant_path	%>
